{
  "name": "scratch-for-traders-frontend",
  "private": true,
  "description": "Build step for the precompiled Tailwind CSS used by index.html, index_1.html, and blog/*.html - see assets/vendor/tailwind/input.css and tailwind.config.js. Nothing here runs at request time; the backend (main.py) has no Node dependency.",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/vendor/tailwind/input.css -o ./assets/vendor/tailwind/tailwind-compiled.css --minify",
    "watch:css": "tailwindcss -i ./assets/vendor/tailwind/input.css -o ./assets/vendor/tailwind/tailwind-compiled.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
